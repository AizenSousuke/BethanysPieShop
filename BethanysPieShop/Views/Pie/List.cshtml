@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model PiesListViewModel

<h1>
    Category: @Model.CurrentCategory
</h1>
@foreach (var pie in Model.Pies)
{
    <div class="card">
        <div class="card-body">
            <img class="card-img" src="@pie.ImageUrl" />
            <h2 class="card-title">@pie.Name</h2>
            <h3 class="card-subtitle text-muted">@pie.Price.ToString("c")</h3>
            <h4 class="card-text">@pie.Category.CategoryName</h4>
            <button class="btn">View Cake</button>
        </div>
    </div>
}